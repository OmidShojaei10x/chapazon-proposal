# راهنمای راه‌اندازی پروپوزال چاپازون

## ✅ چک‌لیست قبل از استفاده

### 1. لوگوها و تصاویر

#### لوگوی یکتانت
- ✅ لوگوی SVG از مسیر `/Users/omid/Downloads/Omid_Shojaei/Yektanet/logo/` کپی شده است
- 📝 **اقدام لازم**: اگر لوگوی PNG نیاز دارید، فایل SVG را به PNG تبدیل کنید
- 📝 **اقدام لازم**: لوگوی PNG را در `assets/images/yektanet-logo.png` قرار دهید

#### لوگوی چاپازون
- ⚠️ **اقدام لازم**: لوگوی چاپازون را از سایت chapazon.com دانلود کنید
- 📝 لوگو را در `assets/images/chapazon-logo.png` قرار دهید
- 📝 فرمت پیشنهادی: PNG با پس‌زمینه شفاف

#### تصویر نمایشگاه (اختیاری)
- 📝 تصویر پس‌زمینه نمایشگاه را در `assets/images/exhibition-bg.jpg` قرار دهید
- 📝 ابعاد پیشنهادی: 1920x1080 یا بزرگتر

---

### 2. اطلاعات ناقص که باید تکمیل شوند

#### شماره غرفه نمایشگاه
در فایل `index.html` موارد زیر را پیدا کرده و جایگزین کنید:

```html
<!-- خط 739 -->
[شماره غرفه] → شماره واقعی غرفه

<!-- خط 743 -->
سالن [X]، غرفه [Y] → سالن و غرفه واقعی

<!-- خط 1234 -->
غرفه [X] → شماره غرفه

<!-- خط 1253 -->
غرفه [X] → شماره غرفه

<!-- خط 1275 -->
سالن [X]، غرفه [Y] → سالن و غرفه واقعی
```

**راهنمای جستجو:**
- در VS Code یا هر ادیتور: `Ctrl/Cmd + F`
- جستجوی: `[شماره غرفه]` یا `[X]` یا `[Y]`

#### هدیه یا مزیت ویژه
```html
<!-- خط 742 -->
🎁 هدیه ویژه برای بازدیدکنندگان
```
این متن را با هدیه واقعی جایگزین کنید، مثلاً:
- "🎁 تخفیف ۲۰٪ برای ثبت‌نام در پلتفرم"
- "🎁 کاتالوگ رایگان محصولات"
- "🎁 مشاوره رایگان"

---

### 3. رنگ‌های برند یکتانت

#### بررسی رنگ‌ها
فایل `css/style.css` را باز کنید و در خط 17-20 رنگ‌ها را بررسی کنید:

```css
:root {
    --primary-color: #FF6B35;      /* رنگ اصلی */
    --secondary-color: #004E89;    /* رنگ ثانویه */
    --accent-color: #F77F00;       /* رنگ تاکیدی */
}
```

#### استخراج رنگ‌های واقعی
1. فایل PDF هویت بصری را باز کنید: `/Users/omid/Downloads/Omid_Shojaei/Yektanet/documents/Yektanet Visual Identity (1).pdf`
2. رنگ‌های Primary, Secondary, Accent را پیدا کنید
3. کدهای HEX رنگ‌ها را در فایل CSS جایگزین کنید

---

### 4. فونت‌های برند

#### فونت Sahel
فونت Sahel یکتانت در مسیر `/Users/omid/Downloads/Omid_Shojaei/Yektanet/fonts/` موجود است.

**اقدام اختیاری**: اگر می‌خواهید از فونت Sahel استفاده کنید:

1. فایل‌های فونت را کپی کنید:
```bash
cp -r /Users/omid/Downloads/Omid_Shojaei/Yektanet/fonts/font\ sahel/* assets/fonts/
```

2. در فایل `css/style.css` فونت را اضافه کنید:
```css
@font-face {
    font-family: 'Sahel';
    src: url('../assets/fonts/Sahel.woff') format('woff');
    font-weight: normal;
}

:root {
    --font-primary: 'Sahel', 'Vazirmatn', sans-serif;
}
```

**نکته**: در حال حاضر از فونت Vazirmatn از Google Fonts استفاده می‌شود که برای فارسی مناسب است.

---

### 5. تست و بررسی

#### تست در مرورگر
1. فایل `index.html` را در مرورگر باز کنید
2. بررسی کنید که:
   - ✅ تمام بخش‌ها نمایش داده می‌شوند
   - ✅ انیمیشن‌ها کار می‌کنند
   - ✅ نمودار بودجه نمایش داده می‌شود
   - ✅ منوی ناوبری کار می‌کند
   - ✅ دکمه "بازگشت به بالا" کار می‌کند

#### تست Responsive
1. Developer Tools را باز کنید (F12)
2. Device Toolbar را فعال کنید (Ctrl/Cmd + Shift + M)
3. در سایزهای مختلف تست کنید:
   - موبایل (375px)
   - تبلت (768px)
   - دسکتاپ (1024px+)

#### تست چاپ
1. دکمه "چاپ پروپوزال" را کلیک کنید
2. یا از `Ctrl/Cmd + P` استفاده کنید
3. بررسی کنید که:
   - ✅ نوار ناوبری نمایش داده نمی‌شود
   - ✅ رنگ‌ها مناسب چاپ هستند
   - ✅ تمام محتوا در صفحات قرار می‌گیرد

---

### 6. بهینه‌سازی (اختیاری)

#### Minify کردن فایل‌ها
برای Production می‌توانید فایل‌های CSS و JS را minify کنید:

```bash
# نصب ابزار minify (اگر نصب نیست)
npm install -g minify

# Minify کردن CSS
minify css/style.css > css/style.min.css

# Minify کردن JS
minify js/script.js > js/script.min.js
```

سپس در `index.html` لینک‌ها را به فایل‌های minified تغییر دهید.

---

## 📝 خلاصه اقدامات ضروری

### قبل از ارائه:
- [ ] لوگوی چاپازون را اضافه کنید
- [ ] شماره غرفه را در تمام جاها جایگزین کنید
- [ ] سالن و غرفه را مشخص کنید
- [ ] هدیه ویژه را مشخص کنید
- [ ] رنگ‌های برند یکتانت را بررسی و در صورت نیاز تغییر دهید
- [ ] در مرورگرهای مختلف تست کنید
- [ ] روی موبایل تست کنید
- [ ] حالت چاپ را تست کنید

### اقدامات اختیاری:
- [ ] تصویر پس‌زمینه نمایشگاه را اضافه کنید
- [ ] فونت Sahel را اضافه کنید
- [ ] فایل‌ها را minify کنید

---

## 🆘 رفع مشکلات

### لوگوها نمایش داده نمی‌شوند
- بررسی کنید که فایل‌ها در مسیر `assets/images/` قرار دارند
- نام فایل‌ها باید دقیقاً مطابق با HTML باشد:
  - `yektanet-logo.png` یا `yektanet-logo.svg`
  - `chapazon-logo.png`

### نمودار بودجه نمایش داده نمی‌شود
- بررسی کنید که Chart.js از CDN لود می‌شود
- Console مرورگر را بررسی کنید (F12) برای خطاهای JavaScript

### انیمیشن‌ها کار نمی‌کنند
- بررسی کنید که AOS.js از CDN لود می‌شود
- بررسی کنید که `js/script.js` درست لود می‌شود

### استایل‌ها اعمال نمی‌شوند
- بررسی کنید که `css/style.css` درست لود می‌شود
- Cache مرورگر را پاک کنید (Ctrl/Cmd + Shift + R)

---

## 📞 پشتیبانی

برای سوالات و مشکلات:
- ایمیل: shojaei@yektanet.ir
- تلفن: 09193619161

---

**موفق باشید! 🚀**

