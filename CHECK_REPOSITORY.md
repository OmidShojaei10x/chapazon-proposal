# ✅ چک‌لیست بررسی Repository و GitHub Pages

از این چک‌لیست استفاده کنید تا مطمئن شوید همه چیز درست است.

## 📋 بررسی Repository

### 1. فایل‌های اصلی
بررسی کنید که این فایل‌ها در repository وجود دارند:

- [ ] `index.html` (در root)
- [ ] `css/style.css`
- [ ] `css/print.css`
- [ ] `js/script.js`
- [ ] `assets/images/yektanet-logo.svg`
- [ ] `assets/images/chapazon-logo.png`
- [ ] `assets/fonts/Sahel.woff`
- [ ] `assets/fonts/Sahel-Bold.woff`
- [ ] `assets/fonts/Sahel-Black.woff`
- [ ] `README.md`

### 2. ساختار فولدرها
ساختار باید به این صورت باشد:
```
chapazon-proposal/
├── index.html
├── css/
│   ├── style.css
│   └── print.css
├── js/
│   └── script.js
├── assets/
│   ├── images/
│   │   ├── yektanet-logo.svg
│   │   └── chapazon-logo.png
│   └── fonts/
│       ├── Sahel.woff
│       ├── Sahel-Bold.woff
│       └── Sahel-Black.woff
└── README.md
```

---

## 🌐 بررسی GitHub Pages

### 1. تنظیمات Pages
در `Settings > Pages` بررسی کنید:

- [ ] **Source**: "Deploy from a branch" انتخاب شده
- [ ] **Branch**: `main` انتخاب شده
- [ ] **Folder**: `/ (root)` یا `/` انتخاب شده
- [ ] دکمه **Save** زده شده

### 2. وضعیت Build
- [ ] در بالای صفحه Pages، یک پیام سبز می‌بینید که می‌گوید:
  - "Your site is live at https://OmidShojaei10x.github.io/chapazon-proposal/"
  - یا "Your site is published at..."

### 3. تست سایت
- [ ] لینک سایت را در مرورگر باز کنید
- [ ] پروپوزال به درستی نمایش داده می‌شود
- [ ] لوگوها نمایش داده می‌شوند
- [ ] استایل‌ها اعمال شده‌اند
- [ ] انیمیشن‌ها کار می‌کنند

---

## 🔍 بررسی دقیق‌تر

### بررسی Actions (اگر مشکلی بود)
1. به تب **"Actions"** در repository بروید
2. آخرین workflow را بررسی کنید
3. اگر خطایی بود، آن را رفع کنید

### بررسی فایل‌ها
1. به تب **"Code"** بروید
2. فایل `index.html` را باز کنید
3. بررسی کنید که:
   - مسیرهای CSS و JS درست هستند: `css/style.css` و `js/script.js`
   - مسیرهای تصاویر درست هستند: `assets/images/...`

---

## ✅ اگر همه چیز درست است

سایت شما باید در آدرس زیر در دسترس باشد:
```
https://OmidShojaei10x.github.io/chapazon-proposal/
```

---

## 🐛 مشکلات رایج

### مشکل: "404 Not Found"
**راه حل**:
- مطمئن شوید `index.html` در root است
- نام فایل باید دقیقاً `index.html` باشد
- چند دقیقه صبر کنید (GitHub Pages ممکن است delay داشته باشد)

### مشکل: "Page build failed"
**راه حل**:
- به تب Actions بروید
- خطاها را بررسی کنید
- معمولاً مشکل از مسیرهای فایل است

### مشکل: تصاویر یا استایل‌ها لود نمی‌شوند
**راه حل**:
- مسیرهای فایل را بررسی کنید (باید نسبی باشند)
- Cache مرورگر را پاک کنید (Ctrl/Cmd + Shift + R)

---

**موفق باشید! 🚀**

